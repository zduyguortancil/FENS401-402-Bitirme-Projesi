# ════════════════════════════════════════════════════════════════
#  Seatwise — Python dependencies   (tested on Python 3.11 – 3.13)
#  Install:   python -m pip install -r requirements.txt
#  A CPU build of PyTorch is sufficient — no GPU is required.
# ════════════════════════════════════════════════════════════════

# ── Web framework / app server ──────────────────────────────────
Flask==3.1.3
Werkzeug==3.1.6

# ── Desktop launchers (run_company.py / run_customer.py) — optional ──
#    If you skip this, run the plain web server instead (see SETUP_GUIDE.txt).
pywebview==6.1

# ── Data handling ────────────────────────────────────────────────
pandas==2.2.3
numpy==2.2.6
pyarrow==22.0.0           # Parquet I/O for pandas
duckdb==1.4.4             # SQL-on-disk for the large datasets

# ── Forecasting & demand models (XGBoost layer) ──────────────────
xgboost==3.1.1
scikit-learn==1.8.0
scipy==1.15.3
joblib==1.5.1
shap==0.51.0              # feature attribution in the manager-analysis panel

# ── Deep learning: Temporal Fusion Transformer + sentiment ───────
torch==2.7.1
pytorch-forecasting==1.6.1
pytorch-lightning==2.6.1
lightning==2.6.1
transformers==4.52.4      # DeBERTa-v3-small sentiment model (auto-downloaded on first run)
sentencepiece             # tokenizer required by DeBERTa-v3

# ── Sentiment data fetching (Google News RSS + GDELT) ────────────
requests==2.32.5

# ── Reporting / plotting ─────────────────────────────────────────
reportlab==4.4.9          # in-app PDF report generation + thesis-figure scripts
matplotlib==3.10.3
